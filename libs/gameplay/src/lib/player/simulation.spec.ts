import { simulatePlayer } from './simulation';

describe('simulatePlayer', () => {
  test('handles movement', () => {});

  test('handles starting spell cast', () => {});

  test('handles spell direction cast', () => {});

  test('handles cast success', () => {});
});

test('takeDamage', () => {});

test('initPlayersState', () => {});
